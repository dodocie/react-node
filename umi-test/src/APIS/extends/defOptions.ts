import { RequestConfig } from 'umi';

export const options = {
  timeout: 1500,
  prefix: 'http://localhost:3000',
  errorConfig: {},
  middlewares: [],
  requestInterceptors: [],
  responseInterceptors: [],
}